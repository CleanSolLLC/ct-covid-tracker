<!DOCTYPE html>
<html>

  <head>
  	<style type="text/css">

       table {
       	width: 1200px;
       }

       th, td {
       	padding: 10px 10px 10px 10px 10px 10px 10px;
       }       


  	</style>


  </head>


  <body>

	<% if current_user.gender_cases.empty? %>

	    <h4><%= current_user.username %>,</h4>

	     You have no data saved yet please click the following link to begin your search: <br>

	<% else %>

			<p>Here is a list of your STATEWIDE COVID-19 data by GENDER</p>
			<br>


			<table>

			  <tr>

			  	<th>Delete</th>
			  	<th>Date</th>
			  	<th>Gender</th>
          		<th>Confirmed Cases</th>
          		<th>Case Change</th>
          		<th>Deaths</th>
          		<th>Death Change</th>
					

			  </tr>
				
					<% @gender_cases.each do |gender| %>
					<tr>
					   <td><%= button_to "Delete", {:controller => :gender_cases, :action => :destroy, :id => gender.id}, :method => 'delete' %></td>					   
					   <td> <%= gender.query_date.strftime('%m/%d/%Y')%></td>
					   <td><%= gender.gender %></td>
             		   <td><%= gender.confirmed_cases %> </td>
			           <td><%= gender.case_dir %> <%= gender.case_change %> </td>
			           <td><%= gender.confirmed_deaths %> </td>
			           <td><%= gender.death_dir %> <%= gender.death_change %> </td>
					</tr>
					<% end %>
			</table>

	<% end %>

	<br>
	<%= link_to "Select Additional Dates", new_user_gender_case_path(current_user) %>
	
	</body>		 
</html>








